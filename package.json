{
  "name": "cdk-ecs-pipeline",
  "version": "1.0.0",
  "description": "AWS CDK ECS Fargate with CodePipeline",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy:vpc": "cdk deploy VpcStack",
    "deploy:all": "cdk deploy --all"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.10.0",
    "aws-cdk": "^2.118.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.118.0",
    "constructs": "^10.3.0"
  }
}
